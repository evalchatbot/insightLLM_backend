# =================================================================
# InsightLLM Backend Environment Configuration
# Copy this file to .env and fill in your actual values
# =================================================================

# Database Configuration (Required)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-public-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Authentication (Required)
JWT_SECRET_KEY=your-super-secret-jwt-key-here-make-it-long-and-random

# LLM Configuration (Required - choose one or both)
# Groq (Recommended for speed - used for chat)
GROQ_API_KEY=your-groq-api-key-here
CHATBOT_LLM_MODEL=llama-3.3-70b-versatile

# Grok API (Required for OCR/PDF evaluation)
GROK_API=your-grok-api-key-here
# Alternative variable name (also supported)
# Grok_API=your-grok-api-key-here

# Grok API Base URL (Optional - defaults to https://api.x.ai/v1)
# GROK_API_BASE_URL=https://api.x.ai/v1

# OpenAI (Alternative)
OPENAI_API_KEY=your-openai-api-key-here

# LLM Provider Selection
LLM_PROVIDER=groq
# Options: "groq" or "openai"

# RAG Configuration (Optional - defaults provided)
USE_ADAPTIVE_RAG=true
USE_MULTI_STEP_RAG=true

# Streaming Configuration (Optional)
ENABLE_STREAMING=true
STREAMING_TIMEOUT=60
MAX_TOKENS=2048

# Performance Settings (Optional)
WORKERS=2
LOG_LEVEL=INFO

# Logging Control (Optional - saves memory when disabled)
ENABLE_LOGGING=true
# Set to "false" to disable all logging and save memory

# Production Performance Settings (for docker-compose.prod.yml)
# WORKERS=4
# UVICORN_LIMIT_CONCURRENCY=2000
# UVICORN_LIMIT_MAX_REQUESTS=50000

# Telemetry - LangSmith (Optional)
LANGSMITH_API_KEY=your-langsmith-api-key-here
LANGSMITH_PROJECT=insightLLM_production
LANGSMITH_TRACING=false

# =================================================================
# Additional Configuration Options
# =================================================================

# Supabase Audience (Optional)
SUPABASE_AUDIENCE=authenticated

# Environment
NODE_ENV=production
# Options: "development", "production"

# CORS Origins (for production)
CORS_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com

# Redis Configuration (if using redis)
# REDIS_URL=redis://redis:6379

# =================================================================
# Deployment Specific
# =================================================================

# Domain Configuration (for nginx)
DOMAIN=your-domain.com
FRONTEND_DOMAIN=https://your-frontend-domain.com

# SSL Certificate paths (if using HTTPS)
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# =================================================================
# Development vs Production Examples
# =================================================================

# Development:
# WORKERS=1
# LOG_LEVEL=DEBUG
# ENABLE_LOGGING=true
# LANGSMITH_TRACING=false
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Production (Memory Optimized):
# WORKERS=4
# LOG_LEVEL=ERROR
# ENABLE_LOGGING=false
# LANGSMITH_TRACING=true
# CORS_ORIGINS=https://your-production-frontend.com

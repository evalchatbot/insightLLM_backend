"""
OCR Agent module (stub).
Handles OCR parsing (future integration).
"""
from typing import Any

class OCRAgent:
    """
    Stub OCR agent for image-to-text parsing.
    """
    def parse_image(self, image: Any) -> str:
        """
        Stub: Accepts image input and returns extracted text (placeholder).
        """
        # In a real implementation, process the image and extract text
        return "[Stub] Extracted text from image."
